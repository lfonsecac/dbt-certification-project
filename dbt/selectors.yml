selectors:
  - name: static_staging_models
    description: "Staging models with static data, not being updated"
    definition:
        method: tag
        value: static
        exclude:
          - 'tag:daily'
